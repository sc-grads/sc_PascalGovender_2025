name: Notify on SQL Changes

on:
  push:
    paths:
      - '**.sql'  # Runs only when SQL files change

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send Notification
        run: echo "🚨 SQL script updated! Review changes in GitHub."
